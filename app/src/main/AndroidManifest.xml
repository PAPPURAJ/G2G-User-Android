<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.blogspot.rajbtc.G2GTechUser">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".contact_us.ContactUs"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"></activity>
        <activity android:name=".profile.MyProfile" />
        <activity android:name=".site_estimate.SiteEstimateActivity" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".manage_asset.ManageAsset" />
        <activity
            android:name=".manage_asset.AddAsset"
            android:theme="@style/NoTitle" />
        <activity android:name=".my_activity.ViewEmployeeDetails" />
        <activity android:name=".employee.EmployeeActivity" />
        <activity android:name=".employee.pager_system.item_recycler.profile_costing_salary_credit_PagerSystem.ViewEmployeeDetails" />
    </application>

</manifest>